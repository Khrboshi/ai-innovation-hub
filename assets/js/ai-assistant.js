// Placeholder AI assistant module.
// Use this module to send/receive messages to your AI endpoint (OpenAI, local model, etc.)
async function queryAssistant(prompt) {
  // Example (pseudo):
  // const res = await fetch('/api/assistant', {method:'POST', body: JSON.stringify({prompt})});
  // const {reply} = await res.json();
  // return reply;
  return `Echo (assistant not connected): ${prompt}`;
}
